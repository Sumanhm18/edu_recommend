# Application Configuration
spring.application.name=education
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/education
spring.datasource.username=postgres
spring.datasource.password=suman
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true

# JWT Configuration
jwt.secret=education-mvp-secret-key-for-career-advisor-application-2025-very-long-secret-key
jwt.expiration=86400000

# OTP Configuration
otp.expiration=300000
otp.provider.url=https://api.textlocal.in/send/

# Firebase Configuration
firebase.config.path=firebase-service-account.json

# Logging
logging.level.com.education=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# File upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# CORS Configuration
cors.allowed.origins=http://localhost:3000,http://localhost:8080

# Gemini AI Configuration
gemini.api.key=AIzaSyBqk_XV3TsYvmMbc0c1bNL4w_SWxQSmTuA
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent

# Jackson Configuration
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.deserialization.fail-on-unknown-properties=false